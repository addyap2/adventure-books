{
  "name": "adventure-book",
  "version": "0.1.0",
  "private": true,
  "description": "Branching adventure stories for English learners, at three CEFR levels, with an eight-language glossary.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "validate": "python3 scripts/build_book.py --series content/ --lexicon content/lexicon.json",
    "dev": "npm run build && npx --yes serve dist"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
